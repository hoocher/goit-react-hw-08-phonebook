import { styled } from 'styled-components';

export const FilterDiv = styled('div')(() => {
  return {
    display: 'flex',
    justifyContent: 'space-between',
    width: '400px',
    margin: '0 auto',
  };
});
