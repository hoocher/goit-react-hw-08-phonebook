import { styled } from 'styled-components';

export const InputDiv = styled('div')(() => {
  return {
    marginBottom: '20px',
    display: 'flex',
    padding: '5px',
    margin: '0 auto',
    width: '400px',
    justifyContent: 'space-between',
  };
});
