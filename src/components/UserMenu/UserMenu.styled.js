import { styled } from 'styled-components';

export const UserMenuDiv = styled('div')(() => {
  return {
    display: 'flex',
    justifyContent: 'space-between',
    width: '400px',
    margin: '0 auto',
  };
});
